#include "Vector.h"
#include "Avledet.h"
#include "Quaternion.h"
#include "VUtils.h"
#include "VUtilsMath.h"
#include <quill/LogMacros.h>

/*
std::ostream& operator<<(std::ostream& st, avledet::util::CSU::Vector2f const& vec) {
    return st << "(" << vec.x << ", " << vec.y << ")";
}

std::ostream& operator<<(std::ostream& st, avledet::util::CSU::Vector2i const& vec) {
    return st << "(" << vec.x << ", " << vec.y << ")";
}

std::ostream& operator<<(std::ostream& st, avledet::util::CSU::Vector2s const& vec) {
    return st << "(" << vec.x << ", " << vec.y << ")";
}

std::ostream& operator<<(std::ostream& st, avledet::util::CSU::Vector3f const& vec) {
    return st << "(" << vec.x << ", " << vec.y << ", " << vec.z << ")";
}

avledet::util::CSU::Vector2<float>::UP;
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::ZERO  = Vector2();
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::UP    = Vector2(0, 1);
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::DOWN  = Vector2(0, -1);
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::LEFT  = Vector2(-1, 0);
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::RIGHT = Vector2(1, 0);
avledet::util::CSU::Vector2 const avledet::util::CSU::Vector2::ONE   = Vector2(1, 1);
*/

