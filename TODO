TODO

## TODO

- You must define the below cmake arguments:

  ![cmake configure args reference](./docs/cmake-args.png)
  - CMAKE_TOOLCHAIN_FILE
  - LUA_INCLUDE_DIR
  - LUA_LIBRARY
- ./vcpkg install lua:x64-linux-dynamic
  - this allows resolution of shared lua for the server and luasocket

- The `-vcpkg.json` must be worked on
  - everything is fine, except for requiring triplet specification for lua (dynamic-linux...)
  - Windows support is assumed to no longer work because of the massive changes (windows is terrible anyhow)

- fix project naming
  - multiple pascal case vs snake case... MAKE UP YOUR MIND DAMNIT
- remove tracy profiler
- fix worldgen
- other stuff thats cool! like better lua...
- prune tmp lua test case scripts


    

    dungeon generator

    ver





    add c++23 install link
        https://dev.to/marcosplusplus/how-to-install-gcc-14-and-use-c23-41od

    Linux:
        I just installed magic-enum via vcpkg, and is working fine...

    windows fixes

    dump new game data packages
        the current ones are VERY outdated



    rework the Rpc derived class
        or migrate from avl...


    start entering other classes
        remove raw & references (replace with std::reference_wrapper / pointer / smarts as needed)

    ORGANIZE!!!
    
    MOVE THINGS OUT OF THE HEADER!!!
        notable offenders:
            Peer.h
            ZDO.h

    script rules:
        do not pass pointers, except for full lifetime types (mans)

        pass shared ptr for managed types

        pass copy-by-value for blahs...

        readonly is 'fake'
            applies to the immediate top-level table only (for returned usertypes)

            better alternative would be a sol::property(getter)
                with getter only



    script reload quirks
        reload on script save

    specific reload functionality
        config option or smth

        reload on script file save
            instead of info save

    fix lua print(er)
        path spec
        primarily quill formatting for file path

    test multi-player functioning

    finalize refactors of get_ / find_ ...
        get implies IT EXISTS (otherwise panic)

        find implies it might not exist, and to recover...

    restructure / synchronize the entire load/re/unload
        functionality, to reduce code duplication

    create google test unit for scripts
        silence the logger,
        use custom logger
        make loggers decentralized? instead of single global AVL_LOGGER
        

    dump all (modern) resources    