/*
#include "DataWriter.h"
#include "DataReader.h"

void DataWriter::Write(DataReader in) {
    //BYTE_VIEW_t(in.data(), in.Length()).data()[0] = 9;
    Write(BYTE_VIEW_t(in.data(), in.Length()));
}*/
